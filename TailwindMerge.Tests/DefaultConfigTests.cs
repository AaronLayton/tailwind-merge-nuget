using Xunit;
using TailwindMerge.Tests;
using TailwindMerge;

namespace TailwindMerge.Tests;

public class DefaultConfigTests
{
    [Fact]
    public void DefaultConfig_HasCorrectTypes()
    {
        //var defaultConfig = DefaultConfig.GetDefaultConfig();

        //Assert.Equal(500, defaultConfig.CacheSize);
        //Assert.Null(defaultConfig.NonExistent);
        //Assert.Equal("block", defaultConfig.ClassGroups.Display[0]);
        //Assert.Equal("auto", defaultConfig.ClassGroups.Overflow[0].Overflow[0]);
        //Assert.Null(defaultConfig.ClassGroups.Overflow[0].NonExistent);
    }
}
